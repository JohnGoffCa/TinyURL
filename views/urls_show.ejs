<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/_head %>
  <% if (url) { %>
    <title>TinyApp - <%= url.url %></title>
  <% } %>
</head>
<body>
  <% include ./partials/_header %>
  <% if (url) { %>
    <% if (user && user.id === url.userID) { %>
      <%= url.url %> - <%= shortURL %>
      <form action="/urls/<%= shortURL %>" method="POST">
        <label for="newURL">Enter a new URL:</label>
        <input id="newURL" type="text" name="newURL" placeholder="http://" style="width: 300px">
        <input type="submit" value="Submit">
      </form>
    <% } else { %>
      <div>This link does not belong to you!</div>
    <% } %>
  <% } else { %>
    <div>That short URL doesn't seem to exist!</div>
  <% } %>
</body>
</html>
